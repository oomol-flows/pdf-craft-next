inputs_def:
  - handle: pdf_path
    description: "Path to the input PDF file"
    json_schema:
      type: string
      ui:widget: file
    nullable: false

  - handle: output_dir
    description: "Directory where the markdown file and assets will be saved"
    json_schema:
      type: string
      ui:widget: dir
    nullable: false

  - handle: includes_footnotes
    description: "Whether to include footnotes in the output markdown"
    json_schema:
      type: boolean
    value: true
    nullable: false

  - group: Optional
    collapsed: true

  - handle: use_vllm
    description: "Enable vLLM backend for high-performance inference (recommended
      for faster processing)"
    json_schema:
      type: boolean
    value: true
    nullable: false

  - handle: gpu_memory_utilization
    description: "GPU memory utilization ratio for vLLM (0.0-1.0). Only used when
      vLLM is enabled"
    json_schema:
      type: number
      minimum: 0.1
      maximum: 1.0
    value: 0.9
    nullable: true

  - handle: generate_plot
    description: "Whether to generate analysis visualizations during conversion"
    json_schema:
      type: boolean
    value: false
    nullable: true

outputs_def:
  - handle: markdown_path
    description: "Path to the generated markdown file"
    json_schema:
      type: string

  - handle: assets_dir
    description: "Directory containing the extracted images and assets"
    json_schema:
      type: string

executor:
  name: python
  options:
    entry: __init__.py
    spawn: false

title: "PDF to Markdown"
icon: ":carbon:document-export:"
description: "Convert PDF documents to Markdown format with image extraction.
  Supports footnotes and generates analysis visualizations."
